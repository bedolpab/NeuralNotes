"""Utilities for Cryptography package."""

from typing import List


def generate_n_length_binary(n: int) -> List[str]:
    """Generate set of n-length strings for some n in N."""
    set_graycode = []

    if n == 0:
        return 0

    return set_graycode
